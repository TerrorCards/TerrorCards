import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.gisgames.terrorcards",
  appName: "Terror Cards",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
