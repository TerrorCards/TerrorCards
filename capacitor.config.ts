import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.terrorcards.app',
  appName: 'TerrorCards',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
